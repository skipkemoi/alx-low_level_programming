#include <stdio.h>
/**
 * main - prints all single digit base 10 numbers
 *
 * Return: 0 (success)
 */
int main(void)
{
	int numb;

	for (numb = 0; numb < 10; numb++)
	{
	printf("%d", numb);
	}
	printf("\n");
	return (0);
}
